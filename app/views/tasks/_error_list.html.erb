<% if @task.errors.any? %>
	<div id="errror_explanation">
		<div class="alert alert-error">
			The form contains <%= pluralize(@task.errors.count, "error") %>.
		</div>
		<ul>
			<% @task.errors.full_messages.each do |error| %>
				<li>* <%= error %> </li>
			<% end %>
		</ul>
	</div>
<% end %>